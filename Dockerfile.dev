# Development Dockerfile using webpack serve in development mode
FROM node:18-alpine

# Set working directory
WORKDIR /app

# Copy package files first to leverage Docker layer caching
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source code
COPY . .

# Expose webpack serve port
EXPOSE 3000

# Start webpack serve in development mode with host binding for Docker
CMD ["npx", "webpack", "serve", "--mode", "development", "--host", "0.0.0.0"]
